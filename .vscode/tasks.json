{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "push them all",
            "type": "shell",
            "command": "git add . ; git commit -m 'update' ; git push origin"
        }
    ]
}